interface:
  display_name: "Clean VM"
  short_description: "Clean stale loop runs, tabs, and worktrees"
  default_prompt: "Use $clean-vm to clean this loop VM without touching active sessions."

policy:
  allow_implicit_invocation: false
